# Supply-chain (guía typescript.md): un paquete malicioso puede ejecutar código
# en `postinstall`. Con esto ningún script de dependencias corre al instalar.
# Validado: el build de Vite sigue OK sin ellos (ningún binario nativo aquí).
ignore-scripts=true
